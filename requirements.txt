# Core scientific stack
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Gradient-boosting surrogates (Cell 7 benchmark families)
catboost>=1.2
xgboost>=2.0
lightgbm>=4.0

# Global sensitivity analysis: Morris + Sobol (Cell 9)
SALib>=1.4

# Causal discovery: PC, GES (Cell 10)
causal-learn>=0.1.3

# NOTEARS linear (Cell 10) — installed from GitHub, not published on PyPI
notears @ https://github.com/xhochy/notears/archive/master.zip

# SHAP attribution (Cell 11)
shap>=0.44

# Symbolic regression via genetic programming (Cell 12)
gplearn>=0.4

# Multi-objective optimization + hyperparameter search (Cells 15a-15d)
pymoo>=0.6
optuna>=3.4
